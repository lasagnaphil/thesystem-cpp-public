{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run (Debug)",
            "type": "shell",
            "command": "bin/debug/thesystem.exe",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "options": {
                "cwd": "${workspaceFolder}/.."
            }
        },
        {
            "label": "Run (Release)",
            "type": "shell",
            "command": "../bin/release/thesystem.exe"
        }
    ]
}