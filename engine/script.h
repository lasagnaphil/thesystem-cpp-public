//
// Created by lasagnaphil on 2021-07-31.
//

#ifndef THESYSTEM_SCRIPT_H
#define THESYSTEM_SCRIPT_H

#include "resources.h"
#include "squirrel.h"

#include <string>

// TODO: Remove this in favor of modules

/*
struct Script {
    static Id<Script> load(HSQUIRRELVM vm, Resources& res, const char* filename);

    SQRESULT reload(HSQUIRRELVM vm);

    void run(HSQUIRRELVM vm);

    std::string filename;
    HSQOBJECT object;
};
 */

#endif //THESYSTEM_SCRIPT_H
